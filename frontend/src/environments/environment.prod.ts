export const environment = {
  production: true,
  // Backend API base URL. If you deploy the backend as a separate Vercel project,
  // set this to that project's URL + /api (e.g. https://recogynze-api.vercel.app/api).
  apiBaseUrl: 'https://recogynze-api.vercel.app/api'
};
